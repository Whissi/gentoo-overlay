# config file for /etc/init.d/salt-api

# The location of the Salt configuration directory. This directory contains
# the configuration files for Salt API.
# Default: "/etc/salt".
#SALT_CONFIGDIR="/etc/salt"

# Specify the location of the pidfile.
# Default: "/run/salt-api.pid"
#SALT_PIDFILE="/run/salt-api.pid"

# You can use this configuration option to pass additional options to the
# start-stop-daemon, see start-stop-daemon(8) for more details.
# Per default we wait 1000ms after we have started the service to ensure
# that the daemon is really up and running.
#SALT_SSDARGS="--wait 1000"

# See salt-api(8) for a list of valid command line options.
SALT_OPTS="--log-level=warning"
