Gentoo's gcc defines FORTIFY_SOURCE on its own so if we want to redefine
FORTIFY_SOURCE we have to undefine it first.

diff -rupN old/pdns-recursor-3.7.2/configure new/pdns-recursor-3.7.2/configure
--- old/pdns-recursor-3.7.2/configure	2015-04-21 15:02:57.000000000 +0200
+++ new/pdns-recursor-3.7.2/configure	2015-05-25 22:10:12.044160541 +0200
@@ -58,8 +58,8 @@ EOF
   fi
 
   # test for fortified source
-  if $CXX $src -c -o a.out -O3 -D_FORTIFY_SOURCE=2; then
-    CF_FORTIFY="-D_FORTIFY_SOURCE=2"
+  if $CXX $src -c -o a.out -O3 -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=2; then
+    CF_FORTIFY="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=2"
   fi
 
   # test for stack protector
